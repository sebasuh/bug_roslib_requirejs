{
  "name": "roslib_requirejs",
  "version": "1.0.0",
  "description": "Show erroneous behaviour of roslibjs when using requirejs",
  "scripts": {
    "start": "http-server webroot/ -o"
  },
  "dependencies": {
    "http-server": "^0.11.1",
    "requirejs": "2.3.6",
    "roslib": "1.0.1"
  },
  "devDependencies": {
    "grunt": "^1.0.4",
    "grunt-cli": "^1.3.2",
    "grunt-contrib-copy": "^1.0.0"
  }
}
